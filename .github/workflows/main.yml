name: DevOps Staging CI/CD

on: [push]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Code Linter
        run: echo "Linting code base... PASSED"
      - name: Build Docker Image
        run: echo "Building container image... SUCCESS"
      - name: Run Automated Unit Tests
        run: echo "Running 14 unit tests... ALL PASSED"
      - name: Deploy to Staging Cluster
        run: echo "Deploying to Staging Environment... DONE"
